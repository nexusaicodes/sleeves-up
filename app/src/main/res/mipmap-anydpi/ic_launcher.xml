<?xml version="1.0" encoding="utf-8"?>
<adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
    <background android:drawable="@drawable/ic_launcher_background" />
    <foreground android:drawable="@drawable/ic_launcher_foreground" />
    <!-- Themed icons (Android 13+) reuse the foreground rather than carrying their own copy
         of the path: the system supplies the background and tints the shape, so the same
         stroke-on-transparent drawable serves both. -->
    <monochrome android:drawable="@drawable/ic_launcher_foreground" />
</adaptive-icon>
